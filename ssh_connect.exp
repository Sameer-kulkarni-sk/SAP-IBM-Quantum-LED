#!/usr/bin/expect -f
set timeout 20
set host [lindex $argv 0]
set password [lindex $argv 1]
set command [lindex $argv 2]

spawn ssh -o StrictHostKeyChecking=no rasqberry@$host $command
expect "password:"
send "$password\r"
expect eof